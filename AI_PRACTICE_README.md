# 🤖 نظام ممارسة اللغة الإنجليزية مع AI

## ✅ تم إنشاء النظام بالكامل!

نظام **مجاني 100%** لممارسة اللغة الإنجليزية مع ذكاء صناعي متقدم.

---

## 📋 خطوات التشغيل

### 1️⃣ **الحصول على Groq API Key** (مجاني تماماً)

1. اذهب إلى: https://console.groq.com
2. سجل بالإيميل (مجاناً - لا يحتاج بطاقة ائتمان)
3. اضغط على "API Keys" من القائمة الجانبية
4. اضغط "Create API Key"
5. أعطها اسم مثل "Competooo Practice"
6. انسخ المفتاح (يبدأ بـ `gsk_...`)

### 2️⃣ **إضافة API Key للمشروع**

افتح ملف `.env.local` في جذر المشروع وضع المفتاح:

```bash
GROQ_API_KEY=gsk_your_key_here
```

**ملاحظة:** الملف `.env.local` تم إنشاؤه تلقائياً في جذر المشروع.

### 3️⃣ **تشغيل SQL Script**

قم بتشغيل ملف SQL لإنشاء الجداول:

```bash
# الملف موجود في:
scripts/sql/034_ai_practice_system.sql
```

**في Neon Database Console:**
1. افتح SQL Editor
2. انسخ محتوى الملف
3. اضغط Run

أو استخدم CLI:
```bash
psql -h your-host -U your-user -d your-db -f scripts/sql/034_ai_practice_system.sql
```

### 4️⃣ **إعادة تشغيل المشروع**

```bash
npm run dev
```

---

## 🎯 كيفية الاستخدام

### للطلاب:

1. **الوصول للصفحة:**
   - من الـ header: زر "🤖 مارس اللغة" (gradient أزرق-بنفسجي)
   - أو الرابط مباشرة: `/student/practice`

2. **بدء المحادثة:**
   - اختر موضوع المحادثة (أو محادثة حرة)
   - اضغط "ابدأ المحادثة"
   - AI سيبدأ بترحيب

3. **المحادثة:**
   - اكتب بالإنجليزية في صندوق الرسائل
   - AI سيرد عليك ويصحح أخطاءك
   - سيقترح كلمات أفضل
   - سيستمر في المحادثة معك

4. **إنهاء الجلسة:**
   - اضغط "إنهاء الجلسة"
   - ستحصل على تقرير مفصل:
     - درجة القواعد (Grammar)
     - درجة المفردات (Vocabulary)
     - درجة الطلاقة (Fluency)
     - نقاط القوة والضعف
     - الأخطاء الشائعة
     - اقتراحات للتحسين

---

## 🗂️ الملفات المُنشأة

### Database:
- `scripts/sql/034_ai_practice_system.sql`
  - `practice_sessions` - جلسات الممارسة
  - `practice_messages` - رسائل المحادثة
  - `practice_stats` - إحصائيات الطلاب

### Server:
- `server/ai-practice-actions.ts`
  - `startPracticeSession()` - بدء جلسة
  - `sendMessage()` - إرسال رسالة
  - `endPracticeSession()` - إنهاء وتقييم
  - `getPracticeSessions()` - جلب الجلسات السابقة
  - `getPracticeStats()` - إحصائيات الطالب

### Frontend:
- `app/student/practice/page.tsx` - الصفحة الرئيسية
- `components/practice-interface.tsx` - واجهة المحادثة

### UI Updates:
- `components/site-header.tsx` - زر في header
- `components/mobile-nav.tsx` - زر في mobile menu

---

## 🎨 المميزات

### ✅ المتاحة الآن:
- 💬 محادثة نصية (Chat)
- 🤖 AI ذكي يفهم السياق
- ✍️ تصحيح تلقائي للأخطاء
- 📊 تقارير مفصلة
- 📈 تتبع التحسن
- 🔥 Streak Days (أيام متتالية)
- 📝 مواضيع محادثة متنوعة
- 🎯 تقييم دقيق (Grammar, Vocabulary, Fluency)

### 🔜 قريباً:
- 🎤 محادثة صوتية (Voice)
- 📱 تطبيق موبايل
- 🏆 نظام النقاط والشارات
- 👥 منافسة مع طلاب آخرين

---

## 📊 Database Schema

### practice_sessions
```sql
- id (UUID)
- student_id (TEXT)
- topic (TEXT) - موضوع المحادثة
- mode (chat/voice)
- duration_seconds
- total_messages
- grammar_score (0-100)
- vocabulary_score (0-100)
- fluency_score (0-100)
- overall_score (0-100)
- strengths (TEXT[])
- weaknesses (TEXT[])
- suggestions (TEXT[])
- common_mistakes (JSONB)
```

### practice_messages
```sql
- id (UUID)
- session_id (UUID)
- role (student/ai)
- content (TEXT)
- has_grammar_errors (BOOLEAN)
- grammar_corrections (JSONB)
- vocabulary_suggestions (JSONB)
```

### practice_stats
```sql
- student_id (TEXT)
- total_sessions
- total_messages
- total_duration_minutes
- avg_grammar_score
- avg_vocabulary_score
- avg_fluency_score
- avg_overall_score
- streak_days
- last_practice_date
```

---

## 🔧 Troubleshooting

### خطأ: "GROQ_API_KEY is not configured"
**الحل:** تأكد من إضافة المفتاح في `.env.local`

### خطأ: "relation practice_sessions does not exist"
**الحل:** قم بتشغيل SQL script (الخطوة 3)

### الرسائل بطيئة
**الحل:** Groq سريع جداً، لكن تحقق من اتصال الإنترنت

### AI لا يرد
**الحل:** 
1. تحقق من صلاحية API Key
2. تحقق من Groq quota (14,400/day)
3. افحص console للأخطاء

---

## 💰 التكلفة

**صفر! مجاني 100%** 🎉

- Groq API: مجاني (14,400 طلب/يوم)
- Web Speech API: مجاني (متصفح)
- Neon Database: مجاني (المستخدم بالفعل)

**14,400 طلب = ~480 جلسة يومياً** (30 رسالة لكل جلسة)

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. افحص console في المتصفح (F12)
2. افحص server logs
3. تأكد من جميع الخطوات أعلاه

---

## 🚀 What's Next?

1. **احصل على Groq API Key** ← أهم خطوة!
2. **شغّل SQL Script**
3. **جرب الميزة!**
4. **شارك التجربة مع الطلاب** 🎉

---

**Made with ❤️ using:**
- Groq AI (Llama 3.1)
- Next.js 15
- React 18
- TypeScript
- Neon PostgreSQL
- TailwindCSS
